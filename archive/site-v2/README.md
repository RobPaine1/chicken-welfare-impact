# Archived: site v2 (calculator + explanation pages)

The two-page calculator that preceded the bare equation page in `docs/`.
`calc.js` holds every constant with its source; `tests/` exercises it
(`npm test` from the repo root). Open `index.html` directly to use it.
